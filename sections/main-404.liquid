<section class="section-padding bg-background">
  <div class="container-custom text-center py-16">
    <h1 class="text-6xl md:text-8xl font-display font-black text-accent mb-4">404</h1>
    <p class="text-xl text-muted-foreground mb-8">{{ 'pages.not_found.title' | t }}</p>
    <a href="{{ routes.root_url }}" class="btn-cta">{{ 'pages.not_found.back_home' | t }}</a>
  </div>
</section>

{% schema %}
{
  "name": "404",
  "settings": []
}
{% endschema %}
